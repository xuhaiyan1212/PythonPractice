1. Ops drops files to \Reports\ToLoad
2. Script runs every 5 mins and go through Toload dir.
3. if any file under ToLoad dir,
   a. Generate random and unique id
   b. rename them to <unique id>_filename, Copy the files to temp and remove from ToLoad dir

4. Go through Temp dir, find files which start with <unique id>
   for each file:
   a.Check if file locked
   b.not locked, extract data and feed into database. Write down filename and ops name
   c.locked, write down the filename, later send email to ITBOPS


5. send emails to ops inform them upload done

6. send email to ITBOPS and ops if any file locked
